use anyhow::Result;

// Read the index, and generate the corresponding tree
// Then generate the commit which should just point to said tree
pub fn create_commit(message: String) -> Result<()> {
    Ok(())
}
